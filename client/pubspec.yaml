name: mindonglody
description: Algorithm based harmonizer
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_audio_recorder: ^0.5.5
  audioplayers: ^0.13.7
  path_provider: ^1.5.1
  file: ^5.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
   - images/